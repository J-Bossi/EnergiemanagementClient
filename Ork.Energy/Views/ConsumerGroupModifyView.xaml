<UserControl x:Class="Ork.Energy.Views.ConsumerGroupModifyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <TextBlock Text="Name" />
            <TextBox Text="{Binding GroupName}" />
            <TextBlock Text="Beschreibung" />
            <TextBox Text="{Binding GroupDescription}" TextWrapping="Wrap"
                     Width="300"
                     Height="200"
                     AcceptsReturn="True" />
            <Button Style="{DynamicResource SquareButtonStyle}" cal:Message.Attach="SaveConsumerGroup($dataContext)"
                    Content="Speichern" />
        </StackPanel>
    </Grid>
</UserControl>